# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository (celda-25) is newly created and contains no source code yet — only a README with the project name. There are no build, lint, or test commands to run.

Once the project takes shape (language, framework, tooling), update this file with:
- The commands to build, lint, test, and run a single test.
- A high-level description of the architecture.
